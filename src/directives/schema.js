import hasRole from './HasRoleDirective';
import isAuthenticated from './IsAuthenticatedDirective';

export default {
  // hasRole, // this schema version is not working so I'm using the legacy version
  isAuthenticated
}