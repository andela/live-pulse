{
  "name": "live-pulse",
  "version": "1.0.0",
  "description": "The server for Live Pulse",
  "main": "server.js",
  "engines": {
    "node": "10.15.3",
    "npm": "6.6.0"
  },
  "scripts": {
    "babel-node": "babel-node ./src/server --presets=env",
    "build": "npm run remove-build && npm run build:server",
    "build:server": "./node_modules/.bin/babel -d ./build ./src -s --copy-files",
    "build-and-start": "npm run build && npm start",
    "heroku-postbuild": "npm run build",
    "remove-build": "./node_modules/.bin/rimraf ./build",
    "start": "NODE_ENV=production ./node_modules/.bin/forever -m 5 ./build/server.js",
    "start:dev": "NODE_ENV=development nodemon --exec npm run babel-node",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andela/live-pulse.git"
  },
  "author": "Chieze Franklin",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/andela/live-pulse/issues"
  },
  "homepage": "https://github.com/andela/live-pulse#readme",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-loader": "^8.0.4",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-es2015": "^6.24.1",
    "bcryptjs": "^2.4.3",
    "dotenv": "^6.1.0",
    "forever": "^0.15.3",
    "graphql-yoga": "^1.17.4",
    "jsonwebtoken": "^8.5.1",
    "lodash": "^4.17.11",
    "moment": "^2.24.0",
    "prisma-client-lib": "^1.32.0",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.5",
    "rimraf": "^2.6.2"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "nodemon": "^1.18.6"
  }
}
